// routes.ts

interface Routes {
    home: string;
    about: string;
    services: string;
    // Add more routes as needed
  }
  
  const routes: Routes = {
    home: '/',
    about: '/about',
    services: '/services',
    // Add more routes as needed
  };
  
  export default routes;
  